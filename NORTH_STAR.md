# North Star

Agent Foundry lets the user manage intent instead of code. It coordinates specialist agents, isolated workspaces, tests, safety review, reports, and receipts so project work becomes supervised and replayable.

V0.1 proves the local control plane without granting real coding autonomy by default.
